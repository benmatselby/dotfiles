# Configure the prefix
unbind C-b
set-option -g prefix C-a
bind-key C-a send-prefix
bind C-a last-window

# Turn on mouse support
setw -g mouse on

# Scroll History
set -g history-limit 30000

# Lower escape timing from 500ms to 50ms for quicker response to scroll-buffer access.
set -s escape-time 50

# Window navigation
bind -n S-Left  previous-window
bind -n S-Right next-window

# Pane navigation
bind -n M-Left select-pane -L
bind -n M-Right select-pane -R
bind -n M-Up select-pane -U
bind -n M-Down select-pane -D

# More appropriate window split bindings
bind | split-window -h
bind - split-window -v
unbind %
unbind '"'

# Titles
set -g set-titles on
set -g set-titles-string '#T'

# Colours
set -g default-terminal "screen-256color"
setw -g mode-style fg=#218380,bg=#77D9D5
setw -g pane-active-border-style fg=#c0362c
setw -g pane-border-style fg=#77D9D5

# Start window numbering at 1 for easier switching
set -g base-index 1
setw -g pane-base-index 1

# Start numbering at 1
set -g base-index 1

# Mac copy/paste
bind-key -T copy-mode-vi c send-keys -X copy-pipe 'reattach-to-user-namespace pbcopy' \; send -X clear-selection

# Status bar
set-option -g status-position top
setw -g status-style bg=#218380
setw -g window-status-current-style fg=#218380,bg=#77D9D5
setw -g status-left " #S -- "
setw -g status-left-length 30

# Clock
setw -g clock-mode-colour colour037

# Messages
setw -g message-command-style fg=#218380,bg=#77D9D5
setw -g message-style fg=#218380,bg=#77D9D5
